agent:
  command: "echo"
  args: ["no-op"]

settings:
  poll_interval: 10s
  branch_prefix: "line/"
  watches: main

concerns:
  - name: security
    prompt: "Review for security vulnerabilities"
  - name: docs
    prompt: "Update documentation for changes"
