agent:
  command: "echo"
  args: ["no-op"]

settings:
  branch_prefix: "line/"
  watches: main

stations:
  - name: security
    prompt: "Review for security vulnerabilities"
  - name: docs
    prompt: "Update documentation for changes"
