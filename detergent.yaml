agent:
  command: "true"

settings:
  poll_interval: 5s

concerns:
  - name: security
    watches: master
    prompt: "Review for security vulnerabilities"
  - name: docs
    watches: security
    prompt: "Update documentation for changes"
